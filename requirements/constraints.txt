# Version constraints proven on the machine that produced the accepted C0-C13
# engineering-readiness evidence. These are the versions the smoke and validate
# runs actually executed under, captured with `pip list --format=freeze`.
#
# The policy is deliberate and narrow: a scientific dependency is NOT upgraded
# casually and never resolved to "latest" at install time. A float kernel that
# changes between torch releases can move a checkpoint hash, and a manifest that
# said ">=" would let that happen silently on the collaborator's machine.
#
# Upper bounds are exclusive on the next major/minor where the project has a
# recorded compatibility reason; otherwise the tested version is pinned exactly.
#
# torch/torchvision are intentionally ABSENT here: their version is chosen by the
# hardware profile (requirements/cpu.txt or requirements/cuda*.txt), and pinning
# them in a shared constraints file would make one host's wheel bind another's.
numpy==2.5.2
scipy==1.18.0
pandas==3.0.5
pyarrow==25.0.0
pydantic==2.13.4
pydantic_core==2.46.4
PyYAML==6.0.3
typer==0.27.1
click==8.4.2
rich==15.0.0
shellingham==1.5.4
pillow==12.3.0
opencv-python-headless==5.0.0.93
timm==1.0.28
safetensors==0.8.0
huggingface_hub==1.27.0
filelock==3.32.2
fsspec==2026.7.0
packaging==26.3
tqdm==4.70.0
requests==2.34.2
certifi==2026.7.22
charset-normalizer==3.4.9
idna==3.18
urllib3==2.7.0
typing_extensions==4.16.0
annotated-types==0.8.0
sympy==1.14.0
mpmath==1.3.0
networkx==3.6.1
Jinja2==3.1.6
markdown-it-py==4.2.0
colorama==0.4.6
matplotlib==3.11.1
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
kiwisolver==1.5.0
pyparsing==3.3.2
