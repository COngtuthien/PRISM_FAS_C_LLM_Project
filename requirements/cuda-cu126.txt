# CUDA 12.6 profile — NVIDIA Ada (RTX 40-series), Ampere (RTX 30-series, A100)
# and Hopper. Retained for a collaborator host that is not Blackwell.
#
#     pip install -r requirements/cuda-cu126.txt
#
# NOT VALIDATED ON THE MACHINE THAT WROTE THIS FILE; see cuda-cu129.txt.
-r base.txt
-c constraints.txt

--extra-index-url https://download.pytorch.org/whl/cu126

torch==2.13.0
torchvision==0.28.0

onnxruntime==1.24.0
